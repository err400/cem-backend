numpy
pandas
librosa
resampy
soundfile
scipy
tqdm
matplotlib
seaborn
birdnetlib
astral
pytz
psutil

# TensorFlow package name depends on CPU architecture. Linux x86_64 has the
# CPU-only package; Linux/macOS ARM64 uses the regular `tensorflow` package.
tensorflow-cpu; platform_machine == "x86_64" or platform_machine == "AMD64"
tensorflow; platform_machine == "aarch64" or platform_machine == "arm64"

# Optional GPU-accelerated inference. Install `birdnet` (official library) to
# enable the ProtoBuf model path. On GPU hosts install `birdnet[and-cuda]`
# instead and replace `tensorflow-cpu` with `tensorflow` above.
# The pipeline auto-detects GPU and falls back to birdnetlib (TFLite/CPU)
# when birdnet is absent.
birdnet